# NASA POWER Korea Climate Explorer 작업 원칙

- NASA POWER에서 받은 원본 데이터와 분석용 가공 데이터를 명확히 구분한다.
- 분석 결과나 누락된 관측값을 임의로 만들어 내지 않는다.
- API 파라미터, 단위, 시간 기준은 NASA POWER 공식 정의를 따른다.
- 데이터 출처, 변수, 단위와 해석상의 주의점을 README.md에 기록한다.
- 하나의 함수가 다운로드, 정제, 분석, 시각화를 동시에 담당하지 않도록 역할을 나눈다.
- 기존 기능을 수정한 뒤에는 기존 테스트가 모두 통과하는지 확인한다.
- 새 분석 기능을 추가할 때는 가능한 경우 해당 동작을 검증하는 테스트도 함께 추가한다.
- raw 파일은 생성 후 가공하지 않으며, 변환 결과는 data/processed 아래에 별도로 저장한다.
- 기존 raw 파일이 있으면 명시적으로 강제하지 않는 한 같은 데이터를 다시 요청하지 않는다.
- 모든 작업이 끝나면 실행 결과, 검증 결과, 변경 파일을 사용자가 이해할 수 있게 요약한다.

